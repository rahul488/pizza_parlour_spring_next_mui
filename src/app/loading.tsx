import { LoadingBox, ImageBox } from '@/components/styles/loading/loading';
import { Box } from '@mui/material';

const Loading = () => {
  return (
    <LoadingBox>
      <ImageBox></ImageBox>
    </LoadingBox>
  );
};
export default Loading;
